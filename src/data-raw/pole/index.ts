import { VIDEOS } from "resources";

export const POLE_ROUTINES: Array<IPoleRoutine> = Object.values(
  VIDEOS.POLE.ROUTINES
);

export const POLE_TRICKS_VIDEOS: Array<IPoleTricksVideo> = Object.values(
  VIDEOS.POLE.TRICKS
);
